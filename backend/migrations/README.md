# migrations — Supabase SQL migration files
# Day 1: schema.sql applied directly in Supabase Dashboard
# Day 2+: new migrations go here as numbered files, e.g. 002_add_sessions.sql
# Never run raw ALTER TABLE in production without a migration file (AGENTS.md §4)
